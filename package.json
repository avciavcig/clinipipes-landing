{
  "name": "clinipipes-landing",
  "version": "2.3.1",
  "description": "CliniPipes landing — clinipipes.v2.3 (Portal V4.1.1 sync)",
  "scripts": {
    "start": "node server.js",
    "sync:legal": "node scripts/sync-legal-from-live.mjs",
    "build:legal": "node scripts/build-legal-pages.mjs",
    "check:demo-env": "node scripts/check-demo-capture-env.mjs",
    "capture:demo": "node scripts/capture-production-demo.mjs",
    "capture:demo:local": "node scripts/capture-demo.mjs",
    "postinstall": "npx playwright install chromium || true"
  },
  "dependencies": {
    "nodemailer": "^6.10.1",
    "playwright": "^1.49.0",
    "qrcode": "^1.5.4",
    "speakeasy": "^2.0.0"
  }
}
